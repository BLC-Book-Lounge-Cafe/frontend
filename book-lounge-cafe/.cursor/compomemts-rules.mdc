---
alwaysApply: true
---

При импорте UI компонентов всегда используй путь `shared/ui`.

Пример правильного импорта:
import { Button, Field, Toast } from 'shared/ui'

Все переиспользуемые компоненты (кнопки, инпуты, модалки, карточки, селекты и т.д.) должны браться исключительно из `shared/ui`.
